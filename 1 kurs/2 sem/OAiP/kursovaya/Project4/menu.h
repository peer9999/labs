enum MENU {
    INPUT = 1,
    SORT,
    DELETE,
    WRITE,
    READ,
    EDIT,
    FIND,
    EXIT = 0
};

void MenuView  (); //функция для отображения меню
int  MenuSelect(); //функция выбора действия

